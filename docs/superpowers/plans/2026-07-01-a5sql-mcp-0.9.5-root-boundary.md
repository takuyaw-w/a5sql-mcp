# a5sql-mcp 0.9.5 Root Boundary Hardening 実装計画

> **Agent 作業者向け:** 必須サブスキル: この計画を task 単位で実行する場合は、`superpowers:subagent-driven-development` 推奨、または `superpowers:executing-plans` を使うこと。進捗管理には checkbox (`- [ ]`) を使う。

**目的:** 既存の root boundary contract を version metadata、テスト、公開 guidance で固定し、0.9.5 として準備する。

**構成:** 現在の read-only asset root 挙動は変更しない。まず focused RED test で repository がまだ 0.9.4 を示していることを確認し、その後 0.9.5 に必要な version metadata と root boundary docs だけを更新する。

**技術スタック:** TypeScript、Vitest、pnpm workspace、MCP TypeScript SDK、Markdown docs。

---

## ファイル構成

- 変更: `packages/cli/test/mcp-server-smoke.test.ts`
  - version lockstep test を 0.9.4 期待から 0.9.5 期待へ更新する。
- 変更: `package.json`
  - workspace version を 0.9.5 に上げる。
- 変更: `packages/parser/package.json`
  - parser package version を 0.9.5 に上げる。
- 変更: `packages/core/package.json`
  - core package version を 0.9.5 に上げる。
- 変更: `packages/cli/package.json`
  - CLI package version を 0.9.5 に上げる。
- 変更: `packages/cli/src/mcp/server.ts`
  - `A5SQL_MCP_SERVER_VERSION` を 0.9.5 に上げる。
- 変更: `packages/cli/test/docs-redaction.test.ts`
  - release-check docs assertion を 0.9.4 redaction wording から 0.9.5 root boundary wording へ変更する。
- 変更: `README.md`
  - release candidate note を 0.9.5 に更新し、root boundary hardening を要約する。
- 変更: `AGENTS.md`
  - root boundary guidance が揃っていることを確認する。0.9.5 contract の説明が不足している場合だけ編集する。
- 変更: `.agents/skills/a5sql-mcp/SKILL.md`
  - root boundary guidance が揃っていることを確認する。0.9.5 contract の説明が不足している場合だけ編集する。

## タスク

### タスク 1: Version Metadata Lockstep

**対象ファイル:**

- 変更: `packages/cli/test/mcp-server-smoke.test.ts`
- 変更: `package.json`
- 変更: `packages/parser/package.json`
- 変更: `packages/core/package.json`
- 変更: `packages/cli/package.json`
- 変更: `packages/cli/src/mcp/server.ts`

- [ ] **Step 1: failing version test を書く**

`packages/cli/test/mcp-server-smoke.test.ts` の最初の test を、0.9.5 を期待する内容に変更する。

```ts
it("reports 0.9.5 version metadata", async () => {
  expect(A5SQL_MCP_SERVER_VERSION).toBe("0.9.5");

  const packageJsonPaths = [
    new URL("../../../package.json", import.meta.url),
    new URL("../../parser/package.json", import.meta.url),
    new URL("../../core/package.json", import.meta.url),
    new URL("../package.json", import.meta.url),
  ];
  const packageJsons = await Promise.all(
    packageJsonPaths.map(async (packageJsonPath) =>
      JSON.parse(await readFile(packageJsonPath, "utf8")),
    ),
  );

  expect(packageJsons.map((packageJson) => packageJson.version)).toEqual([
    "0.9.5",
    "0.9.5",
    "0.9.5",
    "0.9.5",
  ]);
});
```

- [ ] **Step 2: focused test を実行して RED を確認する**

実行:

```bash
rtk pnpm --filter @takuyaw-w/a5sql-mcp test -- mcp-server-smoke.test.ts
```

期待結果: FAIL。`A5SQL_MCP_SERVER_VERSION` と package versions がまだ `0.9.4` のため失敗する。

- [ ] **Step 3: version metadata を 0.9.5 に上げる**

次の files の version field を編集する。

```json
{
  "version": "0.9.5"
}
```

対象ファイル:

```text
package.json
packages/parser/package.json
packages/core/package.json
packages/cli/package.json
```

`packages/cli/src/mcp/server.ts` を編集する。

```ts
export const A5SQL_MCP_SERVER_VERSION = "0.9.5";
```

- [ ] **Step 4: focused test を実行して GREEN を確認する**

実行:

```bash
rtk pnpm --filter @takuyaw-w/a5sql-mcp test -- mcp-server-smoke.test.ts
```

期待結果: PASS。

- [ ] **Step 5: version metadata を commit する**

実行:

```bash
rtk git add package.json packages/parser/package.json packages/core/package.json packages/cli/package.json packages/cli/src/mcp/server.ts packages/cli/test/mcp-server-smoke.test.ts
rtk git commit -m "chore: prepare 0.9.5 root boundary"
```

### タスク 2: Public Root Boundary Guidance

**対象ファイル:**

- 変更: `packages/cli/test/docs-redaction.test.ts`
- 変更: `README.md`
- 変更: `AGENTS.md`
- 変更: `.agents/skills/a5sql-mcp/SKILL.md`

- [ ] **Step 1: failing docs contract test を書く**

`packages/cli/test/docs-redaction.test.ts` の 2 つ目の test を次の内容に置き換える。

```ts
it("documents the 0.9.5 root boundary release check", async () => {
  const docs = await Promise.all(
    PUBLIC_GUIDANCE_FILES.map(async (relativePath) => ({
      relativePath,
      text: await readFile(new URL(relativePath, import.meta.url), "utf8"),
    })),
  );
  const readme = docs.find((doc) => doc.relativePath === "../../../README.md")?.text ?? "";

  expect(readme).toContain("0.9.5");
  expect(readme).toContain("root 未指定");
  expect(readme).toContain("候補提示だけ");
  expect(readme).toContain("必要最小限");

  for (const { relativePath, text } of docs) {
    expect(text, `${relativePath} should document explicit roots`).toContain(
      "roots または A5SQL_MCP_ROOTS",
    );
  }
});
```

- [ ] **Step 2: focused docs test を実行して RED を確認する**

実行:

```bash
rtk pnpm --filter @takuyaw-w/a5sql-mcp test -- docs-redaction.test.ts
```

期待結果: FAIL。README はまだ 0.9.4 release check を説明しており、guidance file の少なくとも 1 つが test の要求する root wording を満たしていない可能性がある。

- [ ] **Step 3: README の release-candidate wording を更新する**

`README.md` の release-candidate section 付近にある現在の 0.9.4 release note を、次の内容と同等の文言に置き換える。

```md
0.9.5 のリリース候補として、通常の検証に加えて package として install した後の MCP 起動まで確認します。

0.9.5 では、`roots` / `A5SQL_MCP_ROOTS` の最小権限境界を再確認します。root 未指定の場合、`search_a5sql_assets` / `read_a5sql_asset` / `parse_a5sql_asset` / `list_a5sql_connections` は OS、home、APPDATA、Wine などの既定候補を探索しません。`detect_a5sql_locations` は候補提示だけを行い、そこで見つかった path を自動で探索 root にしません。A5:SQL の設定ディレクトリ、保存済み SQL ディレクトリ、対象プロジェクトの ER 図や SQL を置いた作業ディレクトリなど、必要最小限の root だけを指定してください。
```

- [ ] **Step 4: 必要な場合だけ AGENTS と skill を揃える**

docs test が `AGENTS.md` で失敗した場合は、次の既存 contract text または同等の説明が含まれるようにする。

```md
`detect_a5sql_locations` は候補提示だけに使う。`search_a5sql_assets` / `read_a5sql_asset` / `parse_a5sql_asset` / `list_a5sql_connections` は、root 未指定時に APPDATA、LOCALAPPDATA、USERPROFILE、home、Wine などの既定候補を探索対象にしない。
```

docs test が `.agents/skills/a5sql-mcp/SKILL.md` で失敗した場合は、次の文言が含まれるようにする。

```md
現在の tool は、起動時に指定されたファイルと、`roots` または `A5SQL_MCP_ROOTS` で許可されたローカル asset を読み取り専用で扱います。
```

- [ ] **Step 5: focused docs test を実行して GREEN を確認する**

実行:

```bash
rtk pnpm --filter @takuyaw-w/a5sql-mcp test -- docs-redaction.test.ts
```

期待結果: PASS。

- [ ] **Step 6: docs contract を commit する**

実行:

```bash
rtk git add packages/cli/test/docs-redaction.test.ts README.md AGENTS.md .agents/skills/a5sql-mcp/SKILL.md
rtk git commit -m "docs: document 0.9.5 root boundary"
```

### タスク 3: Root Boundary Regression Verification

**対象ファイル:**

- 参照: `packages/core/test/assets.test.ts`
- 参照: `packages/cli/test/mcp-asset-tools.test.ts`
- 参照: `packages/cli/test/mcp-server-smoke.test.ts`

- [ ] **Step 1: focused core root-boundary tests を実行する**

実行:

```bash
rtk pnpm --filter @takuyaw-w/a5sql-mcp-core test -- assets.test.ts
```

期待結果: PASS。少なくとも次の test が含まれていることを確認する。

```text
does not search platform or home defaults when roots are omitted
refuses explicit path reads without roots
```

- [ ] **Step 2: focused MCP handler root-boundary tests を実行する**

実行:

```bash
rtk pnpm --filter @takuyaw-w/a5sql-mcp test -- mcp-asset-tools.test.ts
```

期待結果: PASS。少なくとも次の test が含まれていることを確認する。

```text
requires explicit roots for asset ID reads, asset search, and connection listing
does not read explicit paths outside provided roots
does not follow explicit path symlinks outside provided roots
```

- [ ] **Step 3: MCP smoke test を実行する**

実行:

```bash
rtk pnpm --filter @takuyaw-w/a5sql-mcp test -- mcp-server-smoke.test.ts
```

期待結果: PASS。asset / location tool の `tools/list` coverage が含まれていることを確認する。

### タスク 4: Release Candidate Verification

**対象ファイル:**

- Source edit は想定しない。

- [ ] **Step 1: release check を実行する**

実行:

```bash
rtk pnpm release:check
```

期待結果: PASS。

- [ ] **Step 2: published-style check を実行する**

実行:

```bash
rtk pnpm published:check
```

期待結果: PASS。tarball install、MCP startup、`tools/list` が含まれる。

- [ ] **Step 3: final diff と status を確認する**

実行:

```bash
rtk git status --short --branch
rtk git log --oneline -5
```

期待結果: working tree はこの plan の commits 以外 clean。`main` は `origin/main` より ahead。
