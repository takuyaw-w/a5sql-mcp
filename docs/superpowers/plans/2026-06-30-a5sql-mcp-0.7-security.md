# a5sql-mcp 0.7.0 Security / Privacy Hardening Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Harden the 0.7.0 MCP public surface so read/file/asset/search/connection responses consistently avoid returning secrets while documenting least-privilege roots and the DB execution non-goal.

**Architecture:** Keep masking centralized in `packages/core/src/mask.ts`, then verify every public MCP response boundary that surfaces file content, snippets, or connection candidates. Preserve existing successful asset path metadata for compatibility, and use documentation to steer users toward narrow roots.

**Tech Stack:** TypeScript, Vitest, pnpm workspace scripts, MCP SDK, oxlint, oxfmt.

---

## File Structure

- Modify: `packages/core/src/mask.ts`
  - Owns reusable secret masking regexes and `maskSensitiveText`.
- Modify: `packages/core/test/mask.test.ts`
  - Adds focused unit tests for 0.7.0 masking patterns.
- Modify: `packages/core/test/assets.test.ts`
  - Verifies core asset read/search masking for expanded secret forms.
- Modify: `packages/cli/test/mcp-asset-tools.test.ts`
  - Verifies MCP response boundaries for `read_a5sql_file`, `read_a5sql_asset`, `search_a5sql_assets`, `list_a5sql_connections`, and error cases.
- Modify: `packages/cli/test/mcp-server-smoke.test.ts`
  - Updates version assertions and keeps smoke no-secret assertions aligned.
- Modify: `README.md`
  - Adds explicit least-privilege root guidance and 1.0.0 DB execution non-goal wording.
- Modify: `AGENTS.md`
  - Keeps developer/agent security rules aligned with 0.7.0.
- Modify: `.agents/skills/a5sql-mcp/SKILL.md`
  - Updates the project skill to reflect the current MCP tool surface and 0.7.0 privacy expectations.
- Modify: `package.json`
- Modify: `packages/parser/package.json`
- Modify: `packages/core/package.json`
- Modify: `packages/cli/package.json`
- Modify: `packages/cli/src/mcp/server.ts`
  - Version metadata lockstep to `0.7.0`.

## Task 1: Expand Core Masking Tests and Implementation

**Files:**

- Modify: `packages/core/test/mask.test.ts`
- Modify: `packages/core/src/mask.ts`

- [ ] **Step 1: Add failing tests for 0.7.0 secret forms**

Add these tests inside `describe("maskSensitiveText", () => { ... })` in `packages/core/test/mask.test.ts`, after the existing URL userinfo tests:

```ts
it("masks JSON-style quoted secret keys", () => {
  const masked = maskSensitiveText(
    JSON.stringify({
      host: "localhost",
      password: "json-password",
      api_key: "json-api-key",
      refreshToken: "json-refresh-token",
    }),
  );

  expect(masked).toContain('"host":"localhost"');
  expect(masked).toContain('"password":"***"');
  expect(masked).toContain('"api_key":"***"');
  expect(masked).toContain('"refreshToken":"***"');
  expect(masked).not.toContain("json-password");
  expect(masked).not.toContain("json-api-key");
  expect(masked).not.toContain("json-refresh-token");
});

it("masks env export secret assignments", () => {
  const masked = maskSensitiveText(
    ["export API_KEY=env-api-key", "export ACCESS_TOKEN='env-access-token'", "HOST=localhost"].join(
      "\n",
    ),
  );

  expect(masked).toContain("export API_KEY=***");
  expect(masked).toContain("export ACCESS_TOKEN='***'");
  expect(masked).toContain("HOST=localhost");
  expect(masked).not.toContain("env-api-key");
  expect(masked).not.toContain("env-access-token");
});

it("masks authorization header credentials", () => {
  const masked = maskSensitiveText(
    [
      "Authorization: Bearer raw-bearer-token",
      "authorization: Basic raw-basic-token",
      "X-Request-Id: request-1",
    ].join("\n"),
  );

  expect(masked).toContain("Authorization: Bearer ***");
  expect(masked).toContain("authorization: Basic ***");
  expect(masked).toContain("X-Request-Id: request-1");
  expect(masked).not.toContain("raw-bearer-token");
  expect(masked).not.toContain("raw-basic-token");
});

it("masks URL query credentials", () => {
  const masked = maskSensitiveText(
    "jdbc:postgresql://localhost/app?user=alice&password=query-password&ssl=true&token=query-token",
  );

  expect(masked).toContain("password=***");
  expect(masked).toContain("token=***");
  expect(masked).toContain("user=alice");
  expect(masked).toContain("ssl=true");
  expect(masked).not.toContain("query-password");
  expect(masked).not.toContain("query-token");
});

it("masks inline ODBC-style connection string credentials", () => {
  const masked = maskSensitiveText(
    "Driver=PostgreSQL;Server=localhost;User ID=alice;Pwd=odbc-password;Database=app",
  );

  expect(masked).toContain("Server=localhost");
  expect(masked).toContain("User ID=alice");
  expect(masked).toContain("Pwd=***");
  expect(masked).not.toContain("odbc-password");
});
```

- [ ] **Step 2: Run the focused failing test**

Run:

```bash
rtk pnpm --filter @takuyaw-w/a5sql-mcp-core test -- mask.test.ts
```

Expected before implementation: FAIL for JSON-style quoted keys, env export prefixes, authorization headers, and URL query credentials.

- [ ] **Step 3: Add minimal masking rules**

Update `packages/core/src/mask.ts` by adding these constants after `KEY_VALUE_SECRET`:

```ts
const QUOTED_KEY_VALUE_SECRET =
  /(["'])([A-Za-z0-9_. -]*(?:password|passwd|pwd|pass|secret|token|access[_-]?token|refresh[_-]?token|api[_-]?key|private[_-]?key)[A-Za-z0-9_. -]*)\1(\s*:\s*)(["']?)([^;"'\r\n<>&}]+)(["']?)/gi;

const EXPORT_KEY_VALUE_SECRET =
  /\b(export\s+)([A-Za-z0-9_. -]*(?:password|passwd|pwd|pass|secret|token|access[_-]?token|refresh[_-]?token|api[_-]?key|private[_-]?key)[A-Za-z0-9_. -]*)(\s*=\s*)(["']?)([^;"'\r\n<>&]+)(["']?)/gi;

const AUTHORIZATION_CREDENTIAL = /\b(authorization)(\s*:\s*)(bearer|basic)(\s+)([^\r\n]+)/gi;

const URL_QUERY_SECRET =
  /([?&](?:password|passwd|pwd|pass|secret|token|access[_-]?token|refresh[_-]?token|api[_-]?key|private[_-]?key)=)([^&\s"'<>]+)/gi;
```

Then update `maskSensitiveText` so these replacements run before `KEY_VALUE_SECRET`:

```ts
export function maskSensitiveText(input: string): string {
  return input
    .replace(KEYED_CONNECTION_STRING, (_match, key, separator, openQuote, value) => {
      const closeQuote = openQuote && value.endsWith(openQuote) ? openQuote : "";
      return `${key}${separator}${openQuote}***${closeQuote}`;
    })
    .replace(
      QUOTED_KEY_VALUE_SECRET,
      (_match, keyQuote, key, separator, openQuote, _value, closeQuote) => {
        return `${keyQuote}${key}${keyQuote}${separator}${openQuote}***${closeQuote}`;
      },
    )
    .replace(
      EXPORT_KEY_VALUE_SECRET,
      (_match, prefix, key, separator, openQuote, _value, closeQuote) => {
        return `${prefix}${key}${separator}${openQuote}***${closeQuote}`;
      },
    )
    .replace(AUTHORIZATION_CREDENTIAL, (_match, key, separator, scheme, space) => {
      return `${key}${separator}${scheme}${space}***`;
    })
    .replace(URL_QUERY_SECRET, (_match, prefix) => `${prefix}***`)
    .replace(KEY_VALUE_SECRET, (_match, key, separator, openQuote, _value, closeQuote) => {
      return `${key}${separator}${openQuote}***${closeQuote}`;
    })
    .replace(XML_SECRET, (_match, key, attrs) => `<${key}${attrs}>***</${key}>`)
    .replace(CONNECTION_STRING_SECRET, (_match, key, separator, openQuote, _value, closeQuote) => {
      return `${key}${separator}${openQuote}***${closeQuote}`;
    })
    .replace(URL_USERINFO, (_match, prefix) => `${prefix}***@`)
    .replace(PEM_PRIVATE_KEY, (_match, label) => {
      return `-----BEGIN ${label}-----\n***\n-----END ${label}-----`;
    });
}
```

- [ ] **Step 4: Run the focused passing test**

Run:

```bash
rtk pnpm --filter @takuyaw-w/a5sql-mcp-core test -- mask.test.ts
```

Expected: PASS.

- [ ] **Step 5: Commit Task 1**

Run:

```bash
rtk git add packages/core/src/mask.ts packages/core/test/mask.test.ts
rtk git commit -m "test: expand secret masking coverage"
```

## Task 2: Verify Core Asset Read/Search Mask Expanded Secret Forms

**Files:**

- Modify: `packages/core/test/assets.test.ts`

- [ ] **Step 1: Add a failing core asset test**

Add this test after `it("masks URL and DSN credentials when reading assets", ...)` in `packages/core/test/assets.test.ts`:

```ts
it("masks expanded secret forms in asset snippets and read contents", async () => {
  const root = await makeTempDir();
  const sqlPath = path.join(root, "queries", "expanded-secrets.sql");
  await mkdir(path.dirname(sqlPath), { recursive: true });
  await writeFile(
    sqlPath,
    [
      'select \'{"password":"json-password","api_key":"json-api-key"}\' as payload;',
      "Authorization: Bearer raw-bearer-token",
      "jdbc:postgresql://localhost/app?user=alice&password=query-password&token=query-token",
      "Driver=PostgreSQL;Server=localhost;User ID=alice;Pwd=odbc-password;Database=app",
    ].join("\n"),
    "utf8",
  );

  const assets = await searchA5sqlAssets({ roots: [root], query: "expanded" });
  const read = await readA5sqlAsset({ roots: [root], assetId: assets[0]!.id });
  const serialized = JSON.stringify({ assets, read });

  expect(serialized).toContain('"password":"***"');
  expect(serialized).toContain('"api_key":"***"');
  expect(serialized).toContain("Authorization: Bearer ***");
  expect(serialized).toContain("password=***");
  expect(serialized).toContain("token=***");
  expect(serialized).toContain("Pwd=***");
  expect(serialized).not.toContain("json-password");
  expect(serialized).not.toContain("json-api-key");
  expect(serialized).not.toContain("raw-bearer-token");
  expect(serialized).not.toContain("query-password");
  expect(serialized).not.toContain("query-token");
  expect(serialized).not.toContain("odbc-password");
});
```

- [ ] **Step 2: Run the focused test**

Run:

```bash
rtk pnpm --filter @takuyaw-w/a5sql-mcp-core test -- assets.test.ts
```

Expected: PASS if Task 1 masking rules are sufficient. If it fails, adjust only `packages/core/src/mask.ts` and rerun both core tests:

```bash
rtk pnpm --filter @takuyaw-w/a5sql-mcp-core test -- mask.test.ts assets.test.ts
```

Expected after adjustment: PASS.

- [ ] **Step 3: Commit Task 2**

Run:

```bash
rtk git add packages/core/src/mask.ts packages/core/test/assets.test.ts
rtk git commit -m "test: cover masked asset snippets"
```

## Task 3: Verify MCP Response Privacy Boundaries

**Files:**

- Modify: `packages/cli/test/mcp-asset-tools.test.ts`
- Modify if needed: `packages/cli/src/mcp/tool-handlers.ts`

- [ ] **Step 1: Strengthen `read_a5sql_file` test data**

In `packages/cli/test/mcp-asset-tools.test.ts`, update the write payload in `it("masks secrets when reading the configured file", ...)` to:

```ts
await writeFile(
  sqlPath,
  [
    "select * from users where password='raw-password';",
    "token: raw-token",
    "api_key=raw-api-key",
    'select \'{"password":"json-password","api_key":"json-api-key"}\' as payload;',
    "Authorization: Bearer raw-bearer-token",
    "jdbc:postgresql://localhost/app?user=alice&password=query-password&token=query-token",
  ].join("\n"),
  "utf8",
);
```

Extend the expectations in the same test to include:

```ts
expect(result.structuredContent.text).toContain('"password":"***"');
expect(result.structuredContent.text).toContain('"api_key":"***"');
expect(result.structuredContent.text).toContain("Authorization: Bearer ***");
expect(result.structuredContent.text).toContain("password=***");
expect(result.structuredContent.text).toContain("token=***");
expect(JSON.stringify(result.structuredContent)).not.toContain("json-password");
expect(JSON.stringify(result.structuredContent)).not.toContain("json-api-key");
expect(JSON.stringify(result.structuredContent)).not.toContain("raw-bearer-token");
expect(JSON.stringify(result.structuredContent)).not.toContain("query-password");
expect(JSON.stringify(result.structuredContent)).not.toContain("query-token");
```

- [ ] **Step 2: Add MCP asset read/search expanded secret test**

Add this test after `it("searches assets and returns MCP-friendly asset IDs with masked snippets", ...)`:

```ts
it("masks expanded secret forms in MCP asset read and search responses", async () => {
  const root = await makeTempDir();
  const sqlPath = path.join(root, "queries", "expanded-secrets.sql");
  await mkdir(path.dirname(sqlPath), { recursive: true });
  await writeFile(
    sqlPath,
    [
      'select \'{"password":"json-password","api_key":"json-api-key"}\' as payload;',
      "Authorization: Bearer raw-bearer-token",
      "jdbc:postgresql://localhost/app?user=alice&password=query-password&token=query-token",
      "Driver=PostgreSQL;Server=localhost;User ID=alice;Pwd=odbc-password;Database=app",
    ].join("\n"),
    "utf8",
  );

  const { createReadA5sqlAssetHandler, createSearchA5sqlAssetsHandler } = await loadAssetHandlers();

  const search = await createSearchA5sqlAssetsHandler!()({
    roots: [root],
    query: "Authorization",
    kinds: ["sql"],
  });
  const read = await createReadA5sqlAssetHandler!()({
    roots: [root],
    path: sqlPath,
    maxChars: 1000,
  });
  const serialized = JSON.stringify({
    search: search.structuredContent,
    read: read.structuredContent,
  });

  expect(serialized).toContain('"password":"***"');
  expect(serialized).toContain('"api_key":"***"');
  expect(serialized).toContain("Authorization: Bearer ***");
  expect(serialized).toContain("password=***");
  expect(serialized).toContain("token=***");
  expect(serialized).toContain("Pwd=***");
  expect(serialized).not.toContain("json-password");
  expect(serialized).not.toContain("json-api-key");
  expect(serialized).not.toContain("raw-bearer-token");
  expect(serialized).not.toContain("query-password");
  expect(serialized).not.toContain("query-token");
  expect(serialized).not.toContain("odbc-password");
});
```

- [ ] **Step 3: Add MCP error boundary assertions**

In `it("does not read explicit paths outside provided roots", ...)`, change the file content to include a unique secret and add path assertions:

```ts
const uniqueSecret = `outside-secret-${randomUUID()}`;
await writeFile(sqlPath, `select * from secrets where token='${uniqueSecret}';`, "utf8");
```

Extend the expectations in that test:

```ts
const serialized = JSON.stringify(result.structuredContent);
expect(serialized).not.toContain("select * from secrets");
expect(serialized).not.toContain(uniqueSecret);
expect(serialized).not.toContain(sqlPath);
expect(serialized).not.toContain(outsideRoot);
```

Add a new missing-asset error test after it:

```ts
it("does not leak roots or content when asset ID is missing", async () => {
  const root = await makeTempDir();
  const sqlPath = path.join(root, "queries", "contains-secret.sql");
  const uniqueSecret = `missing-asset-secret-${randomUUID()}`;
  await mkdir(path.dirname(sqlPath), { recursive: true });
  await writeFile(sqlPath, `select '${uniqueSecret}' as token;`, "utf8");

  const { createReadA5sqlAssetHandler } = await loadAssetHandlers();
  const result = await createReadA5sqlAssetHandler!()({
    roots: [root],
    assetId: "does-not-exist",
  });
  const serialized = JSON.stringify(result.structuredContent);

  expect(result.structuredContent).toMatchObject({
    found: false,
    assetId: "does-not-exist",
    code: "asset_not_found",
  });
  expect(serialized).not.toContain(uniqueSecret);
  expect(serialized).not.toContain(sqlPath);
  expect(serialized).not.toContain(root);
});
```

- [ ] **Step 4: Strengthen connection public contract assertions**

In `it("reveals only non-secret connection fields when requested", ...)`, extend the config payload:

```ts
        "Authorization: Bearer raw-bearer-token",
        "ODBC_CONNECTION_STRING=Driver=PostgreSQL;Server=localhost;User ID=alice;Pwd=odbc-password;Database=app",
```

Extend the expectations:

```ts
expect(result.structuredContent.connections[0]).not.toHaveProperty("sourcePath");
expect(serialized).not.toContain(root);
expect(serialized).not.toContain(configPath);
expect(serialized).not.toContain("raw-password");
expect(serialized).not.toContain("raw-bearer-token");
expect(serialized).not.toContain("odbc-password");
expect(serialized).not.toContain("postgres://alice:raw-password@localhost/app");
expect(serialized).not.toContain(
  "Driver=PostgreSQL;Server=localhost;User ID=alice;Pwd=odbc-password;Database=app",
);
```

- [ ] **Step 5: Run the focused CLI test**

Run:

```bash
rtk pnpm --filter @takuyaw-w/a5sql-mcp test -- mcp-asset-tools.test.ts
```

Expected: PASS. If it fails because a public MCP handler exposes internal data, adjust only the handler response shaping in `packages/cli/src/mcp/tool-handlers.ts`, then rerun the same command.

- [ ] **Step 6: Commit Task 3**

Run:

```bash
rtk git add packages/cli/test/mcp-asset-tools.test.ts packages/cli/src/mcp/tool-handlers.ts
rtk git commit -m "test: harden MCP privacy boundaries"
```

## Task 4: Update Documentation for Least-Privilege Roots and DB Non-Goal

**Files:**

- Modify: `README.md`
- Modify: `AGENTS.md`
- Modify: `.agents/skills/a5sql-mcp/SKILL.md`

- [ ] **Step 1: Add README least-privilege section**

In `README.md`, after the Codex/Cursor/Claude setup sections and before `## プロンプト例`, add:

```md
## roots の最小権限設定

`A5SQL_MCP_ROOTS` は `search_a5sql_assets`、`read_a5sql_asset`、`list_a5sql_connections` の探索範囲です。指定した root 配下のファイル名、パス、抜粋が MCP レスポンスに含まれ得るため、必要な最小範囲だけを指定してください。

推奨する指定例:

- A5:SQL の設定ディレクトリだけ
- 保存済み SQL を置いたディレクトリだけ
- 対象プロジェクトの ER 図や SQL を置いた作業ディレクトリだけ

避ける指定例:

- ホームディレクトリ全体
- ドライブ全体
- 複数プロジェクトを含む親ディレクトリ

この MCP サーバーはパスワード、トークン、秘密鍵、接続文字列をマスクしますが、root を広げるほどファイル名やディレクトリ構成などのローカル情報も探索対象に入ります。まず狭い root で起動し、必要になった範囲だけ追加してください。
```

- [ ] **Step 2: Add README DB execution non-goal section**

In `README.md`, after the prompt section paragraph that says the server does not execute SQL, add:

```md
## DB 接続と SQL 実行について

1.0.0 までのスコープでは、実際の接続先 DB への接続、SQL 実行、資格情報の復号・表示は行いません。

`generate_sql_select`、`generate_migration_plan`、`compare_a5er_with_live_schema` は、A5:ER 定義や外部から渡された JSON をもとに案や差分を生成するだけです。この MCP サーバー自身が DB に接続して検査したり、生成した SQL を実行したりすることはありません。

将来 DB 実行機能を追加する場合は、読み取り専用クエリ、明示的な許可、監査ログ、タイムアウト、件数制限を別設計で必須条件にします。
```

- [ ] **Step 3: Align AGENTS.md security wording**

In `AGENTS.md`, under `## セキュリティとプライバシー`, ensure these bullets exist:

```md
- `A5SQL_MCP_ROOTS` や tool input の `roots` は、目的に必要な最小範囲を前提に設計・説明する。
- 成功レスポンスで既存 contract として path metadata を返す場合でも、本文、抜粋、エラー、warning に秘密情報を混ぜない。
- 1.0.0 まで、実際の接続先 DB への接続、SQL 実行、資格情報の復号・表示は non-goal として扱う。
```

- [ ] **Step 4: Update project skill current MCP tool list**

In `.agents/skills/a5sql-mcp/SKILL.md`, replace the stale `## 現在の MCP 構成` and `## 将来の MCP 設計候補` sections with:

```md
## 現在の MCP 構成

現在の stdio MCP サーバーは `packages/cli` の `--mcp` モードで提供します。独立した `packages/mcp-server` は使いません。

現在の tool は、起動時に指定されたファイルと、`roots` または `A5SQL_MCP_ROOTS` で許可されたローカル asset を読み取り専用で扱います。

- `describe_a5sql_file`
- `parse_a5sql_file`
- `read_a5sql_file`
- `detect_a5sql_locations`
- `read_a5sql_asset`
- `list_a5sql_connections`
- `search_a5sql_assets`
- `parse_a5sql_asset`
- `list_a5sql_tables`
- `describe_a5sql_table`
- `explain_a5sql_table`
- `list_a5sql_relationships`
- `find_a5sql_tables`
- `find_a5sql_columns`
- `generate_sql_select`
- `generate_mermaid_er_diagram`
- `generate_model_files`
- `generate_schema_markdown`
- `review_a5sql_schema`
- `suggest_schema_changes`
- `compare_a5er_with_live_schema`
- `generate_migration_plan`

これらの tool はローカルファイルを読み取るだけです。接続先 DB へ接続せず、SQL を実行せず、資格情報を復号・表示しません。

`roots` は必要最小限にします。成功レスポンスでは asset path metadata が含まれる場合があるため、ホームディレクトリ全体やドライブ全体を安易に指定しません。

## 将来の MCP 設計候補

以下は未実装の拡張候補です。実装済み API として扱わないでください。

- A5:SQL の内部設定や履歴形式をより深く解釈した検索を追加する。
- 指定された資産を AI 向けに要約して返す。
- 実際の DB 接続や SQL 実行を扱う場合は、読み取り専用クエリ、明示的な許可、監査ログ、タイムアウト、件数制限を別設計で必須にする。
```

- [ ] **Step 5: Run docs checks**

Run:

```bash
rtk pnpm format:check
rg -n "読み取り専用|A5SQL_MCP_ROOTS|SQL 実行|資格情報" README.md AGENTS.md .agents/skills/a5sql-mcp/SKILL.md
```

Expected: `format:check` PASS, and `rg` shows the updated safety guidance in all three files.

- [ ] **Step 6: Commit Task 4**

Run:

```bash
rtk git add README.md AGENTS.md .agents/skills/a5sql-mcp/SKILL.md
rtk git commit -m "docs: document least privilege roots"
```

## Task 5: Bump Version Metadata to 0.7.0

**Files:**

- Modify: `package.json`
- Modify: `packages/parser/package.json`
- Modify: `packages/core/package.json`
- Modify: `packages/cli/package.json`
- Modify: `packages/cli/src/mcp/server.ts`
- Modify: `packages/cli/test/mcp-server-smoke.test.ts`

- [ ] **Step 1: Update package versions**

Change `"version": "0.6.0"` to `"version": "0.7.0"` in:

```text
package.json
packages/parser/package.json
packages/core/package.json
packages/cli/package.json
```

- [ ] **Step 2: Update MCP server version constant**

In `packages/cli/src/mcp/server.ts`, change:

```ts
export const A5SQL_MCP_SERVER_VERSION = "0.6.0";
```

to:

```ts
export const A5SQL_MCP_SERVER_VERSION = "0.7.0";
```

- [ ] **Step 3: Update smoke test version assertions**

In `packages/cli/test/mcp-server-smoke.test.ts`, change the first test to:

```ts
it("reports 0.7.0 version metadata", async () => {
  expect(A5SQL_MCP_SERVER_VERSION).toBe("0.7.0");

  const packageJsonPaths = [
    new URL("../../../package.json", import.meta.url),
    new URL("../../parser/package.json", import.meta.url),
    new URL("../../core/package.json", import.meta.url),
    new URL("../package.json", import.meta.url),
  ];
  const packageJsons = await Promise.all(
    packageJsonPaths.map(async (packageJsonPath) =>
      JSON.parse(await readFile(packageJsonPath, "utf8")),
    ),
  );

  expect(packageJsons.map((packageJson) => packageJson.version)).toEqual([
    "0.7.0",
    "0.7.0",
    "0.7.0",
    "0.7.0",
  ]);
});
```

Also change the second smoke test name from:

```ts
  it("lists 0.6 tools and returns representative structuredContent contracts", async () => {
```

to:

```ts
  it("lists current tools and returns representative structuredContent contracts", async () => {
```

- [ ] **Step 4: Run smoke test**

Run:

```bash
rtk pnpm --filter @takuyaw-w/a5sql-mcp test -- mcp-server-smoke.test.ts
```

Expected: PASS.

- [ ] **Step 5: Commit Task 5**

Run:

```bash
rtk git add package.json packages/parser/package.json packages/core/package.json packages/cli/package.json packages/cli/src/mcp/server.ts packages/cli/test/mcp-server-smoke.test.ts
rtk git commit -m "chore: bump version to 0.7.0"
```

## Task 6: Final Verification and Release Check

**Files:**

- Read only unless verification reveals a failure in files modified by Tasks 1-5.

- [ ] **Step 1: Run targeted package tests**

Run:

```bash
rtk pnpm --filter @takuyaw-w/a5sql-mcp-core test -- mask.test.ts assets.test.ts
rtk pnpm --filter @takuyaw-w/a5sql-mcp test -- mcp-asset-tools.test.ts mcp-server-smoke.test.ts
```

Expected: PASS for both commands.

- [ ] **Step 2: Run placeholder and raw-secret scans on changed test/docs content**

Run:

```bash
rg -n "raw-password|raw-token|raw-api-key|json-password|json-api-key|raw-bearer-token|query-password|query-token|odbc-password" README.md AGENTS.md .agents/skills/a5sql-mcp/SKILL.md packages/core/src/mask.ts packages/core/test/mask.test.ts packages/core/test/assets.test.ts packages/cli/test/mcp-asset-tools.test.ts packages/cli/test/mcp-server-smoke.test.ts
```

Expected: matches for synthetic raw secret strings only in test input and `not.toContain(...)` expectations. No matches in README, AGENTS.md, or `.agents/skills/a5sql-mcp/SKILL.md`.

- [ ] **Step 3: Run final release gate**

Run:

```bash
rtk pnpm release:check
```

Expected: PASS for format, lint, build, test, typecheck, and pack checks.

- [ ] **Step 4: Inspect git status and commit log**

Run:

```bash
rtk git status --short --branch
rtk git log --oneline --decorate --max-count=8
```

Expected: clean worktree, local branch ahead of `origin/main` by the existing 0.6.0 commit plus 0.7.0 implementation commits. No push is performed.

- [ ] **Step 5: Report completion**

Report:

```text
0.7.0 implementation completed.
Verification: rtk pnpm release:check passed.
Push: not performed.
```

Include the commit subjects created during the plan.
