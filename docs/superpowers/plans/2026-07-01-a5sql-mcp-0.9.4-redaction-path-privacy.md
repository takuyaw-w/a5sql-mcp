# a5sql-mcp 0.9.4 Redaction and Path Privacy Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Fix and verify redaction and path privacy for representative credential formats and MCP tool outputs.

**Architecture:** Preserve the existing read-only MCP output contract. Add focused tests for credential masking and path privacy, then make minimal changes in core extraction logic only where public output can still expose complete connection strings.

**Tech Stack:** TypeScript, Vitest, pnpm workspace, MCP TypeScript SDK.

---

## File Structure

- Modify: `packages/core/test/mask.test.ts`
  - Add a single combined representative credential masking case.
- Modify: `packages/core/test/connections.test.ts`
  - Add RED coverage that connection string keys are not exposed as public candidate fields.
- Modify: `packages/cli/test/mcp-asset-tools.test.ts`
  - Add RED coverage across read/search/parse/list connection output and error path privacy.
- Create: `packages/cli/test/docs-redaction.test.ts`
  - Check README / docs / AGENTS / skill for forbidden local path and credential markers.
- Modify: `packages/core/src/mask.ts`
  - Treat connection-string keys as secret-like keys for connection extraction.
- Modify: `packages/core/src/connections.ts`
  - Ensure connection string keys are recorded only as secret indicators, not public fields.
- Modify: `package.json`, `packages/parser/package.json`, `packages/core/package.json`, `packages/cli/package.json`, `packages/cli/src/mcp/server.ts`
  - Bump versions to `0.9.4`.
- Modify: `README.md`, `AGENTS.md`, `.agents/skills/a5sql-mcp/SKILL.md`
  - Update release candidate wording from `0.9.3` to `0.9.4` and document the redaction audit.

## Tasks

- [x] Add RED tests for representative credential redaction and connection string privacy.
- [x] Run focused tests and confirm they fail for the missing 0.9.4 behavior.
- [x] Implement the minimal connection extraction hardening.
- [x] Add docs redaction audit coverage.
- [x] Bump versions and docs to `0.9.4`.
- [x] Run focused tests, `rtk pnpm release:check`, and `rtk pnpm published:check`.
