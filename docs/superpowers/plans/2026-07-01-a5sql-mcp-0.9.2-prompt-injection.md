# a5sql-mcp 0.9.2 Prompt Injection Defense Baseline Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Add a baseline prompt-injection defense signal to representative A5:SQL-derived MCP outputs.

**Architecture:** Keep the parser and tool schemas unchanged. Add `contentIsUntrusted: true` to read-only helper outputs that return A5:SQL-derived payload, keep existing raw read / parse handler signals, and cover prompt-injection strings with synthetic fixtures.

**Tech Stack:** TypeScript, Vitest, pnpm workspace, MCP handler helpers.

---

## File Structure

- Modify: `packages/cli/test/mcp-tools.test.ts`
  - Add a synthetic `.a5er` fixture containing prompt-injection text and assert representative helper outputs carry `contentIsUntrusted: true`.
- Modify: `packages/cli/test/mcp-asset-tools.test.ts`
  - Add SQL asset search / parse assertions for `contentIsUntrusted: true` and fixed trusted metadata.
- Modify: `packages/cli/src/mcp/tool-outputs.ts`
  - Add a small helper for `contentIsUntrusted: true` and apply it to read-only `.a5er` exploration outputs.
- Modify: `packages/cli/src/mcp/tool-handlers.ts`
  - Add `contentIsUntrusted: true` to `search_a5sql_assets` results.
- Modify: `package.json`, `packages/parser/package.json`, `packages/core/package.json`, `packages/cli/package.json`
  - Bump versions to `0.9.2`.
- Modify: `README.md`, `AGENTS.md`, `.agents/skills/a5sql-mcp/SKILL.md`
  - Tighten the 0.9.2 contract wording from optional to representative-output baseline.

## Tasks

- [ ] Add RED tests for `.a5er` helper outputs and asset search / parse prompt-injection fixtures.
- [ ] Run focused tests and confirm they fail because the missing signals are not present.
- [ ] Implement `contentIsUntrusted: true` for the missing read-only outputs.
- [ ] Bump package versions to `0.9.2`.
- [ ] Update README / AGENTS / skill wording.
- [ ] Run focused tests, then `rtk pnpm release:check` and `rtk pnpm published:check`.
