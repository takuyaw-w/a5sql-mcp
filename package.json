{
  "name": "@a5sql-mcp/workspace",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@11.7.0",
  "scripts": {
    "build": "pnpm -r build",
    "local": "node packages/cli/dist/index.js",
    "pack:check": "pnpm --filter @a5sql-mcp/parser pack --dry-run && pnpm --filter @a5sql-mcp/core pack --dry-run && pnpm --filter a5sql-mcp pack --dry-run",
    "test": "pnpm -r test",
    "typecheck": "pnpm -r typecheck"
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "typescript": "catalog:",
    "vitest": "catalog:"
  }
}
